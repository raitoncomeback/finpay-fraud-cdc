SELECT
    transaction_id,
    account_id,
    user_id,
    merchant_id,
    location_id,
    device_id,
    amount,
    currency,
    txn_type,
    txn_status,
    entry_mode,
    card_present,
    is_3ds,
    is_tokenized,
    description,
    initiated_at,
    authorized_at,
    settled_at,
    user_email,
    user_kyc_status,
    user_risk_score,
    user_account_type,
    user_account_balance,
    merchant_name,
    merchant_category,
    merchant_mcc_code,
    merchant_country,
    merchant_city,
    merchant_risk_level,
    txn_country,
    txn_city,
    txn_lat,
    txn_lon,
    is_online,
    device_fingerprint,
    device_type,
    device_os,
    device_is_trusted,
    device_first_seen_at,
    txn_date,
    txn_hour,
    is_high_amount,
    is_card_not_present,
    is_new_device
FROM {{ source('risingwave', 'silver_transactions_enriched') }}
